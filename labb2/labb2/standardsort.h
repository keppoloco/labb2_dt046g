#pragma once
#ifndef STANDARDSORT_H
#define STANDARDSORT_H
#include <vector>
#include <cassert>
#include <algorithm>

void standardsort(std::vector<int>::iterator first, std::vector<int>::iterator last);

#endif
